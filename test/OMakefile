OCAML_LIBS = ../lib/gpr

OCamlProgram(save_data, gen_data save_data)
OCamlProgram(test_derivatives, test_derivatives)

.DEFAULT: save_data.opt test_derivatives.opt

InstClean()

clean:
	rm -rf data/*
