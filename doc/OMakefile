NAME = gpr

gpr.pdf: gpr.bib

LaTeXDocument($(NAME), gpr)

.DEFAULT: $(NAME).pdf

.PHONY: clean
clean:
	rm -f $(NAME).dvi $(NAME).log $(NAME).aux $(NAME).idx $(NAME).toc
	rm -f $(NAME).bbl $(NAME).blg $(NAME).pdf missfont.log $(NAME).fls
	rm -f *.omc
