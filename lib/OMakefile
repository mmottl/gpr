SOURCES[] =
  interfaces
  utils
  kernel_impl
  fitc

section
  OCAMLCFLAGS += -for-pack Gpr
  OCAMLOPTFLAGS += -for-pack Gpr
  $(addsuffixes $(OCAML_SUFFIXES), $(SOURCES)):

OCamlPackage(gpr, $(SOURCES))
OCamlLibrary(gpr, gpr)

InstClean()
